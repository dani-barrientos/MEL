
/**
 * main tests execution.
 * command line:
 *  -list list all available test
 *  -t <NAME>  execute given test
 *  -a run all tests
 * eachs test can have has it's own command line arguments
 * if both -a and -t are present, all test for given test are executed
 **/
int testsMain(int argc,const char* argv[]);
